import { View } from "react-native";
import React from "react";

const Border = () => (
  <View className="my-2">
    <View
      style={{
        height: 1,
        backgroundColor: "grey",
      }}
    />
  </View>
);

export default Border;
